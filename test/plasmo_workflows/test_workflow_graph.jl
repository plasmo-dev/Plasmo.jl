using Plasmo

workflow = Workflow()

# w1 = add_node!(workflow)
# w2 = add_node!(workflow)
# w3 = add_node!(workflow)
#
# getnodes(workflow)
# collectnodes(workflow)
#
# #add_edge! will not be exposed.  Use connect so you can add port information
# comm1 = add_edge!(workflow,w1,w2)  #should this setup default ports?
# getconnectedfrom(workflow,comm1)
# getconnectedto(workflow,comm1)
